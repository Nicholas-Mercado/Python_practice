import re

# re.search() se if string matches regex

# re.findall() extract portions of a string that match a regex

nfile = open('freecodecamp/mbox_s.txt')
# for line in nfile:
#   line = line.rstrip()
#   if re.search('^From:', line):
#     print(line)
    

